namespace {{ namespace }}
{
{{ metadata|description(1) }}
    public class {{ name }} : {{ parent_name }}
    {
        [JsonPropertyName({{ "{:?}"|format(tag_json_name ) }})]
        public string {{ tag_field_name }} { get => {{ "{:?}"|format(tag_value) }}; }
{% for field in fields %}
{{ field.metadata|description(2) }}
        [JsonPropertyName({{ "{:?}"|format(field.json_name) }})]
        public {{ field.type_ }} {{ field.name }} { get; set; }
{% endfor %}
    }
}
