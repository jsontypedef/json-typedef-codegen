{{ metadata|description(0) }}
public class {{ name }} {
    @JsonValue
    private {{ type_ }} value;

    public {{ name }}() {
    }

    @JsonCreator
    public {{ name }}({{ type_ }} value) {
        this.value = value;
    }

    public {{ type_ }} getValue() {
        return value;
    }

    public void setValue({{ type_ }} value) {
        this.value = value;
    }
}
