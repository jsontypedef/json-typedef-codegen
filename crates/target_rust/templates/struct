{{ metadata|description(0) }}
#[derive(Serialize, Deserialize)]
pub struct {{ name }} {
{% for field in fields %}
{{ field.metadata|description(1) }}
    #[serde(rename = {{ "{:?}"|format(field.json_name) }})]
    {% if field.optional %}#[serde(skip_serializing_if = "Option::is_none")]{% endif %}
    pub {{ field.name }}: {{ field.type_ }},
{% endfor %}
}
