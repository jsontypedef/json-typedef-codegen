{{#each aliases~}}
{{ comment 80 "/**" " * " " */" description }}
export type {{name}} = {{value}};

{{/each~}}
{{#each structs~}}
{{ comment 80 "/**" " * " " */" description }}
export interface {{name}} {
{{#each members~}}
{{ comment 80 "  /**" "   * " "   */" description }}
  {{name}}{{#unless required}}?{{/unless}}: {{value}};
{{/each~}}
}

{{/each~}}
