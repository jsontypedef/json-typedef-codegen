{
    "metadata": {
        "integration": {
            "python": {
                "MAIN_CLASS": "Enum1",
                "CODEGEN_DIR": "enum_"
            }
        }
    },
    "enum": [
        "foo",
        "bar",
        "baz"
    ]
}
