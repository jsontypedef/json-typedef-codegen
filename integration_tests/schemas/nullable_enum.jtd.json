{
    "metadata": {
        "integration": {
            "python": {
                "MAIN_CLASS": "NullableEnum1",
                "CODEGEN_DIR": "nullable_enum"
            }
        }
    },
    "enum": [
        "foo",
        "bar",
        "baz"
    ],
    "nullable": true
}
