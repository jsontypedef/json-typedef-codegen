{
    "metadata": {
        "integration": {
            "python": {
                "MAIN_CLASS": "Enum_",
                "CODEGEN_DIR": "enum_"
            },
            "typescript": {
                "MAIN_CLASS": "Enum",
                "CODEGEN_DIR": "enum"
            }
        }
    },
    "enum": [
        "foo",
        "bar",
        "baz"
    ]
}
