{
    "metadata": {
        "integration": {
            "python": {
                "MAIN_CLASS": "Enum_",
                "CODEGEN_DIR": "enum_"
            }
        }
    },
    "enum": [
        "foo",
        "bar",
        "baz"
    ]
}
